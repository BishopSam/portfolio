class LiveProject {
  final String title, description;
  final String? appStoreUrl, playStoreUrl;

  LiveProject( {required this.title,required this.description,required this.appStoreUrl, required this.playStoreUrl});
  
}